# 项目展示与导师交流提纲

## 一句话介绍

这是一个 54 家门店、33 个商品品类、16 天预测跨度的零售需求预测研究。重点不是追求排行榜，而是证明多步预测中的信息集定义、时间回测、模型比较和复现流程都经得起检查。

## 方法亮点

- 把训练样本明确为“预测起点 × 门店 × 品类 × 预测跨度”，用一个含 `horizon` 特征的汇总直接预测模型完成 1–16 天预测。
- 所有销量历史特征都截止到对应预测起点；未来 16 天内的真实销量不会反馈到同一预测块。
- 使用三个预先固定的开发回测起点选择模型和 HGB 迭代次数，再对 2017-07-31 至 2017-08-15 做一次独立终测。
- 在同一信息集上比较八周同星期均值、Ridge 和 HistGradientBoosting，并按跨度、品类及间歇性诊断误差。
- 用合成数据测试未来目标扰动、分组隔离、滚动窗口边界、精确滞后日期及跨度 1/16 的语义。

## 可报告的结果

HGB 的三个开发折平均 RMSLE 为 0.447500，三个折都优于 Ridge。独立终测 RMSLE 为 0.433648；季节基线为 0.520631，Ridge 为 0.635423。HGB 在终测的 16 个跨度和 33 个品类上都取得更低的配对平方对数损失。跨度误差不是单调上升，因此不应把结果表述为“预测越远必然越差”。

## 应主动说明的边界

- 结果是预测性证据，不是促销或门店属性的因果效应。
- 三个开发窗口和一个终测窗口不能覆盖所有制度变化或突发事件。
- 间歇性品类和尖峰需求仍然困难；当前分析只诊断问题，没有引入专门模型。
- 没有可核验的 Kaggle 排行榜成绩，因此只报告本地时间回测和提交文件完整性。

## 推荐展示顺序

先讲真实 16 天预测时能看到什么信息，再解释旧式逐日滞后为何会改变训练与推理语义；随后展示滚动回测、独立终测、跨度误差和品类误差。最后运行测试与提交检查，证明结论能从原始数据复现。

